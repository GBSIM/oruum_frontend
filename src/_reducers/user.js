
const initialState = {
    name: "지원",
    totalAsset: 5143235125,
    totalAssetChange: 3012122,
    totalAssetChangePercentage: 1.51,
}

const user = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default user;